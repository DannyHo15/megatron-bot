# Copy to .env and fill in. NEVER commit .env.
OPENAI_API_KEY=
VECTOR_STORE_ID=            # required for the daily job — the store the Assistant points to
ZENDESK_BASE_URL=https://support.optisigns.com
ZENDESK_LOCALE=en-us
